services:
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      db:
        condition: service_healthy

  db:
    image: postgres:16-alpine
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: ${DB_NAME:-gestorcom}
      POSTGRES_USER: ${DB_USER:-gestorcom}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-gestorcom_dev}
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-gestorcom}"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
